import { UsersExeptionFilter } from './users-exeption.filter';

describe('UsersExeptionFilter', () => {
  it('should be defined', () => {
    expect(new UsersExeptionFilter()).toBeDefined();
  });
});
